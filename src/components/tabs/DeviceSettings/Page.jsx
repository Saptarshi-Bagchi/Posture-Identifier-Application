import { Icon } from '../../dashboard/Icon'
import { card, SectionTitle } from '../../posturesync/Shared'

export default function DeviceSettingsPage({ device, telemetry, telemetryStatus, onNavigate }) {
  const endpoint = `${telemetryStatus?.host ?? '127.0.0.1'}:${telemetryStatus?.port ?? 1883}`
  return <div className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-wider text-[#8eb69b]">Device</p><h1 className="mt-1 text-2xl font-bold">MQTT connection</h1><p className="mt-2 text-sm text-[#8eb69b]">Embedded aMQTT broker status and current ESP32 source.</p></div><section className={card}><SectionTitle eyebrow="Broker" title={telemetryStatus?.listening ? 'Listening for MQTT telemetry' : 'Broker unavailable'} action={<span className={telemetryStatus?.listening ? 'text-emerald-300' : 'text-red-300'}>●</span>} /><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-xl bg-[#051f20] p-4"><p className="text-xs text-[#8eb69b]">Protocol</p><p className="mt-2 font-bold">MQTT</p></div><div className="rounded-xl bg-[#051f20] p-4"><p className="text-xs text-[#8eb69b]">Endpoint</p><p className="mt-2 font-bold">{endpoint}</p></div><div className="rounded-xl bg-[#051f20] p-4"><p className="text-xs text-[#8eb69b]">Error</p><p className="mt-2 text-sm font-bold">{telemetryStatus?.error || 'None'}</p></div></div><button type="button" onClick={() => onNavigate('Live Data')} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-[#8eb69b] px-4 py-3 text-xs font-bold text-[#051f20]"><Icon name="live" size={16} />Open live data</button></section><section className={card}><SectionTitle eyebrow="Current device" title={telemetry ? device.id : 'No telemetry received'} />{telemetry ? <p className="text-sm text-[#8eb69b]">Topic: <span className="font-bold text-[#daf1de]">posture/{telemetry.device_id}/telemetry</span></p> : <p className="text-sm text-[#8eb69b]">Publish telemetry to <span className="font-bold text-[#daf1de]">posture/&lt;device_id&gt;/telemetry</span>.</p>}</section></div>
}
